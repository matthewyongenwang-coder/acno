# Data pipeline
kagglehub>=1.0

# Training. The current recipe is PyTorch (scripts/train_torch.py), which runs
# on Apple Silicon via MPS and is roughly 5x faster than the TensorFlow CPU
# path. TensorFlow is still needed for the original scripts/train_classifier.py
# and for converting those older Keras weights in scripts/convert_models.py.
torch>=2.2
torchvision>=0.17
tensorflow>=2.16
ultralytics>=8.2
scikit-learn>=1.4
matplotlib>=3.8

# ONNX export and the parity check against the PyTorch/Keras originals.
# tf2onnx converts the older Keras classifiers in scripts/convert_models.py.
onnx>=1.16
onnxruntime>=1.17
tf2onnx>=1.16

# Image and array handling used across the scripts
numpy>=1.26
pillow>=10.0

# Face detection in the Python reference pipeline (src/pipeline.py).
# Pinned below 5: OpenCV 5 removed the Haar cascade face detector we use.
opencv-python-headless>=4.9,<5
